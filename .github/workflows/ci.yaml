# THIS FILE WAS AUTOMATICALLY GENERATED, PLEASE DO NOT EDIT.
#
# Generated on 2023-10-03T09:33:23Z by kres 3b6dc55-dirty.

name: default
concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
"on":
  push:
    branches:
      - main
      - release-*
    tags:
      - v*
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - labeled
    branches:
      - main
      - release-*
jobs:
  default:
    permissions:
      contents: write
      packages: write
    runs-on:
      - self-hosted
      - generic
    if: ${{ (!startsWith(github.head_ref, 'renovate/') || !startsWith(github.head_ref, 'dependabot/')) && (github.event.label == null || (github.event.label.name == 'e2e' || github.event.label.name == 'e2e-test')) }}
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: Unshallow
        run: |
          echo "${{ toJSON(github.event) }}"
          echo "${{ (!startsWith(github.head_ref, 'renovate/') || !startsWith(github.head_ref, 'dependabot/')) && (github.event.action != 'labeled' || github.event.label.name == 'e2e') }}"
  test:
    needs:
      - default
    runs-on:
      - self-hosted
      - generic
    if: contains(github.event.pull_request.labels.*.name, 'e2e') || contains(github.event.pull_request.labels.*.name, 'e2e-test')
    steps:
      - name: checkout
        uses: actions/checkout@v3
