# THIS FILE WAS AUTOMATICALLY GENERATED, PLEASE DO NOT EDIT.
#
# Generated on 2023-10-03T09:33:23Z by kres 3b6dc55-dirty.

name: default
concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
"on":
  push:
    branches:
      - main
      - release-*
    tags:
      - v*
  pull_request:
    branches:
      - main
      - release-*
jobs:
  default:
    permissions:
      contents: write
      packages: write
    runs-on:
      - self-hosted
      - generic
    if: (!startsWith(github.head_ref, 'renovate/') && !startsWith(github.head_ref, 'dependabot/'))
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: retrieve labels
        uses: potiuk/get-workflow-origin@v1_5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
        id: workflow-run-info
    outputs:
      labels: ${{ steps.workflow-run-info.outputs.pullRequestLabels }}
  test:
    if: contains(needs.default.outputs.labels, 'integration/short')
    needs:
      - default
    runs-on:
      - self-hosted
      - generic
    steps:
      - name: checkout
        uses: actions/checkout@v3
